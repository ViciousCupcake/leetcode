package src;

public class App {
    public static void main(String[] args) throws Exception {


        /*while(true) {
            int[] arr = new int[(int) Math.pow(2, 28)];
            // initialize w/ random values so compiler doesnt try to optimize anythign
            // weirdly
            for (int a = 0; a < arr.length; a++) {
                arr[a] = (int) (Math.random() * Integer.MAX_VALUE);
            }


            long start = System.currentTimeMillis();
            for (int b = 0; b < arr.length; b++) {
                int lmao = arr[b];
                continue;
            }
            System.out.println("used w/ assigning index: " + (System.currentTimeMillis()-start));
            start = System.currentTimeMillis();
            for (int lmao : arr) {
                continue;
            }
            System.out.println("used w/ for each " + (System.currentTimeMillis()-start));
        }*/

    }
}
